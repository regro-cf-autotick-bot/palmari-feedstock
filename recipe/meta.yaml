{% set name = "palmari" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/palmari-{{ version }}.tar.gz
  sha256: 2b4ab3c1ed3d3edf7e742d0ab8b4fa8f7dd3fb53fd8cd9061cb46be499c0a889
  patches:
    - qtpy.patch  # Use qtpy instead of PyQt5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
  run:
    - click
    - dask-core >=2022.1.0
    - dask-image >=2021.12.0
    - imageio-ffmpeg
    - magicgui
    - matplotlib-base >=3.5
    - munkres
    - napari
    - nd2reader
    - numpy
    - pandas
    - python >=3.8
    - pyyaml
    - qtpy
    - scikit-image >=0.18.3
    - scikit-learn
    - tifffile
    - toml
    - tqdm
    - trackpy >=0.5.0

test:
  imports:
    - palmari
  commands:
    - pip list
  requires:
    - pip

about:
  home: https://github.com/hippover/palmari
  summary: A pipeline for PALM movies analysis (pre-processing, localization, drifft correction, tracking)
  license: LGPL-2.1-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - hippover
    - jaimergp
